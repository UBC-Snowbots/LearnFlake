# Pinned-version snapshot of pyproject.toml deps for reproducibility.
# Authoritative source is /pyproject.toml at repo root. This file exists for
# `pip install -r` workflows and to match the rover_gpu container baseline.

numpy>=1.24,<3
scipy>=1.11
mujoco>=3.6.0
gymnasium>=1.0,<2
stable-baselines3>=2.7,<3
sb3-contrib>=2.7,<3
dm-control>=1.0.20
h5py>=3.10
tqdm>=4.66
tensorboard>=2.15
PyYAML>=6.0

# DEV
pytest>=7.4
pytest-cov>=4.1
ruff>=0.4

# NOTE: torch 2.10.0+cu128 (or newer cu128 nightly) is required for RTX 5060
# / sm_120. The rover_gpu container already has it. If installing on a fresh
# host:
#   pip install --pre torch torchvision --index-url https://download.pytorch.org/whl/nightly/cu128
